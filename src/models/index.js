const Prize = require('./prize');
const GachaHistory = require('./gachaHistory');

// Jika kamu punya model lain seperti user-schema atau books-schema,
// kamu bisa daftarkan juga di sini agar seragam.

module.exports = {
  Prize,
  GachaHistory,
};
